This directory holds test code for IEEE tests on standards surrounding NaN's and inifnity. 
Simply run the Makefile ('make') to compile it, and ./Driver to run.
To install CUnit, type: sudo apt-get install libcunit1 libcunit1-doc libcunit1-dev
